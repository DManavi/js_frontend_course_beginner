function add(n, m) {
  const result = n + m;
  return result;
}

const addFunc = function (n, m) {
  return n + m;
};

const addFuncArrow = (n, m) => {
  return n + m;
};

const addFuncArrowSimple = (n, m) => n + m;

// console.log(addFuncArrowSimple(2, 3));

///////////////////////////////////
// const myResult = add(2, 3);
// console.log(myResult);
// alert(myResult)

///////////////////////////////////
// const myResult2 = add(myResult, 5);
// console.log(myResult2);
// alert(myResult2)
